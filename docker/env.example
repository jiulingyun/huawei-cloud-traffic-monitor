# Docker Compose env example (place or copy to docker/.env if desired)

# Encryption key for sensitive fields (generate a strong key)
ENCRYPTION_KEY=your_encryption_key_here

# App settings
APP_ENV=production
APP_DEBUG=false

# Database
# For local SQLite: sqlite:///./data/monitor.db
# For production, use a managed DB URL (Postgres/MySQL): e.g. postgresql://user:pass@db:5432/monitor
DATABASE_URL=sqlite:///./data/monitor.db
# Disable SQL query echo in production by setting DATABASE_ECHO=false
DATABASE_ECHO=false


